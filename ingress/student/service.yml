apiVersion: v1
kind: Service
metadata: 
  name: studentsrv
spec:
  type: ClusterIP
  selector:
    app: studentdeploy
  ports:
    - port: 8080
      targetPort: 8080
      name: tomcat
      protocol: TCP